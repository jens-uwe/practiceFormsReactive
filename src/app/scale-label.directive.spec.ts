import { ScaleLabelDirective } from './scale-label.directive';

describe('ScaleLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleLabelDirective();
    expect(directive).toBeTruthy();
  });
});
